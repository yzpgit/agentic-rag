# ===== RAGAS 评测独立依赖 =====
# 与主项目隔离，避免 langchain/torch 等重依赖污染运行镜像
# 安装：pip install -r requirements-eval.txt
#
# 依赖链说明：
#   ragas → langchain + langchain-openai + langchain-community + datasets
#   总体积约 300MB（不含 torch，因为只用 API 不用本地模型）

ragas>=0.2.0
langchain-openai>=0.1.20
# 注意：必须 pin <0.4，否则 ragas 导入 langchain_community.chat_models.vertexai 会失败
# https://github.com/vibrantlabsai/ragas/issues/2741
langchain-community>=0.3.0,<0.4
langchain-core>=0.2.0
datasets>=2.20
pandas>=2.0
