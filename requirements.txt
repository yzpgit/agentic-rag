# ===== Web =====
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.1
python-multipart>=0.0.9

# ===== 配置 =====
PyYAML>=6.0

# ===== 文档解析 =====
pypdf>=4.3              # PDF
beautifulsoup4>=4.12    # HTML
python-docx>=1.1        # DOCX

# ===== 切分 =====
# 使用内置实现，无需额外依赖

# ===== Embedding / 向量库 =====
# 向量检索使用云 API（讯飞/通义/OpenAI 兼容接口），无需本地模型
# 默认向量库用 numpy 实现，无需 faiss/torch（节省 2GB+ 下载）
numpy>=1.26
rank-bm25>=0.2.2         # BM25 关键词检索
# 可选：Chroma 向量数据库（HNSW 索引，大规模数据性能更好）
# 启用方法：取消下行注释，并在 config.yaml 中设置 retriever.vector.provider: chroma
# chromadb>=0.5.0

# ===== LLM =====
openai>=1.40             # OpenAI / 兼容接口（longcat/qwen/讯飞 均可走此）

# ===== 以下为阶段2+ 依赖，阶段1可注释 =====
# langgraph>=0.2
# FlagEmbedding>=1.2     # bge-m3 / bge-reranker
# qdrant-client>=1.10
# ragas>=0.1
